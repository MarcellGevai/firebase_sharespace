# Project Rules

- **Auto-Deploy:** After completing any task that modifies the codebase, automatically commit the changes, push them to the repository, and run `npm run deploy` without needing to ask the user. You can start the deployment process as a background task.
